name: senda
description: "Senda - Medición Offline"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  flutter_map: ^8.3.2
  flutter_map_tile_caching_plus: ^11.0.0
  geolocator: ^11.0.0
  latlong2: ^0.9.1
  share_plus: ^9.0.0
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true